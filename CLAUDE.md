# price-offers — מערכת קבועה להצעות מחיר

תיקייה זו מפיקה הצעות מחיר של סטודיו מטרה (הסטודיו של רון). קובץ זה מתעד את הכללים הקבועים לכל הצעה חדשה.

## מבנה התיקייה

```
price-offers/
  template.html         ← השלד הקבוע. אל תשנה עיצוב/מבנה/סקריפט בו, רק תוכן בעת יצירת עותק.
  quotes/                ← כל הצעה בפועל, קובץ נפרד per לקוח.
    elazar.html
  assets/
    signature-ron.png    ← חתימת רון בכר, מוטבעת קבוע בכל הצעה.
```

## איך יוצרים הצעת מחיר חדשה

1. העתק את `template.html` ל-`quotes/<שם-לקוח>.html`.
2. מלא את כל השדות המסומנים ב-`[סוגריים מרובעים]`: שם לקוח, תאריך, שם פרויקט, תיאור, שורות טבלת ההשוואה, מחירים.
3. עדכן תוכן בלבד: פסקאות, טבלת ההשוואה, SEO, תוספות, מתנות, אחסון. אל תיגע ב-CSS, במבנה ה-HTML הכללי, בסקריפט החתימה, בלוגו, או בבלוק האישור.
4. ודא שהנתיב ל-`assets/signature-ron.png` נכון יחסית למיקום הקובץ (מ-`quotes/` זה `../assets/signature-ron.png`).
5. עדכן את ה-`<title>` ואת תאריך חתימת הסטודיו בבלוק האישור.

## כללי עיצוב קבועים (אל תשנה בין הצעות)

- עברית מלאה, `dir="rtl"` ו-`direction: rtl` בכל המסמך.
- גופן Heebo מ-Google Fonts.
- פלטת צבעים מינימליסטית: שחור/אפור לטקסט, סגול בודד (`--accent`) כאקסנט ל"מומלץ" ולפרטי קשר בלבד.
- מסמך להדפסה, לא אתר: בלי אנימציות, בלי אפקטים מיותרים, בלי גרדיאנטים.
- טבלת ההשוואה היא מרכז המסמך: 3 עמודות בדסקטופ, חבילה מומלצת מודגשת, הופכת לכרטיסים בנייד (מתחת ל-720px).
- print CSS שומר על הדפסה נקייה ל-PDF (page-break-inside: avoid בסעיפים קריטיים).
- ניסוח: בלי מקפים ארוכים (—), בלי המילה "מלא"/"מלאה" כתיאור שיווקי, ניסוח נייטרלי ועובדתי.
- אחרי כל שינוי עיצובי: הרץ `/impeccable polish` על הקובץ שהשתנה.

## חתימה דיגיטלית ואישור לקוח

- בצד הסטודיו: `assets/signature-ron.png` מוטבעת קבוע עם השם "רון בכר, סטודיו מטרה" ותאריך ההצעה.
- בצד הלקוח: `signature_pad.js` (טעון מ-CDN) עם canvas לחתימה, שדות שם/אימייל/תאריך, וכפתור "אני מאשר את ההצעה".
- בלחיצה על הכפתור: החתימה מומרת ל-Base64 PNG (`canvas.toDataURL`) ונשלחת כשדה טקסט `signature_image` בתוך בקשת JSON רגילה (`Content-Type: application/json`) אל Web3Forms, יחד עם שם, אימייל, תאריך ושם ההצעה.
- **חשוב: אין לשלוח את החתימה כקובץ מצורף (`multipart/form-data` עם Blob/File).** נבדק בפועל וזה נכשל עם השגיאה `"You are trying to use a Pro feature, Please upgrade to use file uploads."` — צירוף קבצים ב-Web3Forms דורש תוכנית בתשלום. הפתרון היחיד שעובד בתוכנית החינמית הוא Base64 כטקסט.
- שדה `email` בטופס מוגדר לאימייל שהלקוח הזין, כדי שה-Reply-To/Autoresponder של Web3Forms יוכל לשלוח אליו עותק אישור.
- נבדק ואומת בפועל: שליחה מלאה מ-`quotes/elazar.html` עברה בהצלחה מול ה-Access Key האמיתי (תגובת API: `success:true`).

### הגדרת Web3Forms

- Access Key פעיל כבר מוטבע ב-`template.html` וב-`quotes/elazar.html` (משתנה `WEB3FORMS_ACCESS_KEY` בתוך `<script>` הסמוך ל-`</body>`): `88de6efc-90cc-4d9d-8f87-4fcde9b4d8ad`, רשום תחת wendydigitalstudios@gmail.com.
- מכיוון שהמפתח כבר בתוך `template.html`, כל עותק חדש שנוצר ממנו יורש אותו אוטומטית. אין צורך להזין אותו שוב פר לקוח, אלא אם המפתח יוחלף בעתיד.
- כדי שהלקוח יקבל עותק אישור אוטומטי למייל שהזין, יש להפעיל בדשבורד Web3Forms (תחת הגדרות הטופס של אותו Access Key) את אפשרות ה-Autoresponder ולוודא שהיא ממוענת לשדה `email`. זו הגדרה חד-פעמית באתר Web3Forms, לא בקוד.
- אם המפתח אי פעם יוחלף: לעדכן בבת אחת גם ב-`template.html` וגם בכל הקבצים הקיימים תחת `quotes/`.

### בדיקה לפני שליחת הצעה ללקוח אמיתי

- למלא את הטופס ידנית (חתימה, שם, אימייל) ולוודא שהמייל אכן מגיע ל-wendydigitalstudios@gmail.com.
- לבדוק שגם עותק האישור מגיע לכתובת שהוזנה בשדה האימייל של הלקוח.

## דברים שלא לעשות

- לא לשנות את מבנה ה-CSS/HTML של `template.html` בעת מילוי הצעה ספציפית — רק תוכן.
- לא להשאיר Access Key ריק או placeholder בקובץ שנשלח ללקוח בפועל.
- לא להוסיף אנימציות, גרדיאנטים, אימוג'ים, או אלמנטים דקורטיביים שלא ביקש הלקוח.
- לא למחוק או לשנות את `assets/signature-ron.png`.
