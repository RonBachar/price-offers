# price-offers — מערכת קבועה להצעות מחיר

תיקייה זו מפיקה הצעות מחיר של סטודיו מטרה (הסטודיו של רון). קובץ זה מתעד את הכללים הקבועים לכל הצעה חדשה.

## מבנה התיקייה

```
price-offers/
  template.html         ← השלד הקבוע. אל תשנה עיצוב/מבנה/סקריפט בו, רק תוכן בעת יצירת עותק.
  quotes/                ← כל הצעה בפועל, קובץ נפרד per לקוח.
    elazar.html
  assets/
    signature-ron.png    ← חתימת רון בכר, מוטבעת קבוע בכל הצעה.
```

## איך יוצרים הצעת מחיר חדשה

1. העתק את `template.html` ל-`quotes/<שם-לקוח>.html`.
2. מלא את כל השדות המסומנים ב-`[סוגריים מרובעים]`: שם לקוח, תאריך, שם פרויקט, תיאור, שורות טבלת ההשוואה, מחירים.
3. עדכן תוכן בלבד: פסקאות, טבלת ההשוואה, SEO, תוספות, מתנות, אחסון. אל תיגע ב-CSS, במבנה ה-HTML הכללי, בסקריפט החתימה, בלוגו, או בבלוק האישור.
4. ודא שהנתיב ל-`assets/signature-ron.png` נכון יחסית למיקום הקובץ (מ-`quotes/` זה `../assets/signature-ron.png`).
5. עדכן את ה-`<title>` ואת תאריך חתימת הסטודיו בבלוק האישור.

## כללי עיצוב קבועים (אל תשנה בין הצעות)

- עברית מלאה, `dir="rtl"` ו-`direction: rtl` בכל המסמך.
- גופן Heebo מ-Google Fonts.
- פלטת צבעים מינימליסטית: שחור/אפור לטקסט, סגול בודד (`--accent`) כאקסנט ל"מומלץ" ולפרטי קשר בלבד.
- מסמך להדפסה, לא אתר: בלי אנימציות ובלי אפקטים מיותרים.
- **חריג יחיד לכלל "בלי גרדיאנטים": בלוק הסגירה (`.closing`).** הלקוח ביקש שם במפורש רקע דרמטי, גרדיאנט סגול-שחור אלכסוני עם טקסט לבן. זה מכוון ומאושר, אין "לתקן" אותו חזרה לרקע בהיר. בכל שאר המסמך אין גרדיאנטים.
- כפתור "אני מאשר את ההצעה": סגול (`--accent`) קבוע, ובריחוף מתהפך לשחור (`--ink`). לא להפוך את הכיוון.
- תוויות אזור החתימה הן "הסטודיו" ו"הלקוח" בלבד, בלי "מאשר מטעם".
- טבלת ההשוואה היא מרכז המסמך: 3 עמודות בדסקטופ, חבילה מומלצת מודגשת, הופכת לכרטיסים בנייד (מתחת ל-720px).
- print CSS שומר על הדפסה נקייה ל-PDF (page-break-inside: avoid בסעיפים קריטיים).
- ניסוח: בלי מקפים ארוכים (—), בלי המילה "מלא"/"מלאה" כתיאור שיווקי, ניסוח נייטרלי ועובדתי.
- אחרי כל שינוי עיצובי: הרץ `/impeccable polish` על הקובץ שהשתנה.

## חתימה דיגיטלית ואישור לקוח

- בצד הסטודיו: `assets/signature-ron.png` מוטבעת קבוע עם השם "רון בכר, סטודיו מטרה" ותאריך ההצעה.
- בצד הלקוח: `signature_pad.js` (טעון מ-CDN) עם canvas לחתימה, שדות שם/אימייל/תאריך, וכפתור "אני מאשר את ההצעה".

### הארכיטקטורה: Netlify Forms (ראשי) + Web3Forms (עותק ללקוח)

- **החתימה עצמה נשלחת כקובץ PNG אמיתי דרך Netlify Forms.** הטופס הוא `<form name="quote-approval" data-netlify="true" enctype="multipart/form-data">` עם `<input type="file" name="signature">`.
- ב-submit: `canvas.toBlob()` יוצר PNG, נעטף ב-`File`, ומוזרק ל-FormData דרך `fd.set('signature', file, file.name)`. ה-POST הולך ל-`window.location.pathname`.
- ה-`<input type="file">` חייב להישאר ב-HTML הסטטי (גם אם מוסתר) — Netlify מזהה שדות טפסים בזמן ה-deploy מתוך ה-HTML. בלעדיו שדה הקובץ לא ייקלט.
- שדות חובה שאסור להסיר: `form-name` (נדרש לשליחת AJAX), ו-honeypot `bot-field`.
- הצפייה בחתימה: דשבורד Netlify ← Forms ← quote-approval ← הורדת ה-PNG.

### מגבלות שנבדקו בפועל, אל תחזור עליהן

- **Web3Forms לא תומך בצירוף קבצים בתוכנית החינמית.** ניסיון לשלוח Blob/File כ-`multipart/form-data` נכשל עם `"You are trying to use a Pro feature, Please upgrade to use file uploads."`
- **Base64 בגוף המייל אינו פתרון טוב.** נבדק ועבד טכנית, אבל המחרוזת מגיעה כטקסט ארוך שלקוחות מייל שוברים לשורות, כך שהיא עלולה לא להיפתח כתמונה. לכן ננטש לטובת Netlify.
- **`fetch` ל-`file://` מחזיר 200 OK ומתעלם מה-POST.** זה יצר "אישור" שקרי כשפותחים את הקובץ מקומית. לכן קיימת בקוד בדיקת `location.protocol` שחוסמת שליחה מחוץ ל-http/https. **אל תסיר אותה.**
- מסקנה מעשית: **אי אפשר לבדוק את הטופס מקומית.** הבדיקה האמיתית היחידה היא באתר שהועלה ל-Netlify.

### מה הלקוח מקבל אחרי החתימה

- מיד אחרי אישור מוצלח, אזור החתימה מוחלף בתצוגה סטטית: תמונת החתימה, שם הלקוח ותאריך. הטופס עצמו נעלם.
- נפתח אוטומטית דיאלוג ההדפסה של הדפדפן, והלקוח שומר את ההצעה החתומה כ-PDF. נשאר גם כפתור "שמירת ההצעה החתומה כ-PDF" למקרה שסגר את הדיאלוג.
- **למה `window.print()` ולא ספריית PDF:** html2canvas/jsPDF מרסטרות את הדף, פוגעות באיכות ושוברות עברית RTL ומעברי עמודים. ההדפסה המובנית משתמשת ב-print CSS שכבר מכוון למסמך, ומפיקה PDF וקטורי נקי. אין להחליף זאת בספריית PDF.
- שם הלקוח מוזרק דרך `textContent` ולא `innerHTML`, כי זה קלט חופשי. אין לשנות ל-innerHTML.

### עותק אישור ללקוח

- **המערכת אינה שולחת ללקוח את המסמך החתום במייל.** דף סטטי לא יכול לצרף קובץ למייל לכתובת שרירותית; לשם כך נדרשת Netlify Function + שירות מייל (Resend וכדומה). זה לא מומש. הלקוח מקבל את המסמך דרך שמירת ה-PDF בדפדפן (ראה למעלה).
- Netlify Forms בתוכנית החינמית **אינו שולח autoresponder ללקוח**, רק התראה לכתובת קבועה.
- לכן לאחר שליחה מוצלחת ל-Netlify מתבצעת קריאה שנייה ל-Web3Forms (JSON, בלי החתימה) שמטרתה היחידה היא עותק האישור ללקוח. שדה `email` מוגדר לכתובת שהלקוח הזין.
- הקריאה השנייה אינה חוסמת: אם היא נכשלת, האישור מול Netlify עדיין תקף.
- לכיבוי: `SEND_CLIENT_COPY = false` בראש הסקריפט.

### הגדרות חד-פעמיות בדשבורד (לא בקוד)

1. **Netlify ← Forms ← Notifications**: הוסף התראת אימייל ל-wendydigitalstudios@gmail.com, אחרת שליחות ייאספו בדשבורד בלי שתדע.
2. **Web3Forms ← Autoresponder**: הפעל, כדי שהלקוח יקבל את עותק האישור.
3. מגבלת התוכנית החינמית של Netlify Forms: 100 שליחות בחודש.

### הגדרת Web3Forms

- Access Key פעיל כבר מוטבע ב-`template.html` וב-`quotes/elazar.html` (משתנה `WEB3FORMS_ACCESS_KEY` בתוך `<script>` הסמוך ל-`</body>`): `88de6efc-90cc-4d9d-8f87-4fcde9b4d8ad`, רשום תחת wendydigitalstudios@gmail.com.
- מכיוון שהמפתח כבר בתוך `template.html`, כל עותק חדש שנוצר ממנו יורש אותו אוטומטית. אין צורך להזין אותו שוב פר לקוח, אלא אם המפתח יוחלף בעתיד.
- כדי שהלקוח יקבל עותק אישור אוטומטי למייל שהזין, יש להפעיל בדשבורד Web3Forms (תחת הגדרות הטופס של אותו Access Key) את אפשרות ה-Autoresponder ולוודא שהיא ממוענת לשדה `email`. זו הגדרה חד-פעמית באתר Web3Forms, לא בקוד.
- אם המפתח אי פעם יוחלף: לעדכן בבת אחת גם ב-`template.html` וגם בכל הקבצים הקיימים תחת `quotes/`.

### בדיקה לפני שליחת הצעה ללקוח אמיתי

- למלא את הטופס ידנית (חתימה, שם, אימייל) ולוודא שהמייל אכן מגיע ל-wendydigitalstudios@gmail.com.
- לבדוק שגם עותק האישור מגיע לכתובת שהוזנה בשדה האימייל של הלקוח.

## דברים שלא לעשות

- לא לשנות את מבנה ה-CSS/HTML של `template.html` בעת מילוי הצעה ספציפית — רק תוכן.
- לא להשאיר Access Key ריק או placeholder בקובץ שנשלח ללקוח בפועל.
- לא להוסיף אנימציות, גרדיאנטים, אימוג'ים, או אלמנטים דקורטיביים שלא ביקש הלקוח.
- לא למחוק או לשנות את `assets/signature-ron.png`.
